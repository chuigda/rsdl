[private]
[inline]
int = native(
  rust => "i64",
  typescript => "number"
)

[private]
[inline]
float = native(
  rust => "f64",
  typescript => "number"
)

[private]
[inline]
bool = native(
  rust => "bool",
  typescript => "boolean"
)

[private]
[inline]
str = native(
  rust => "String",
  typescript => "string"
)
