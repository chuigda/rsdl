[private]
[inline]
int = native(
  rust => "i64",
  typescript => "number",
  julia => "Int64"
)

[private]
[inline]
float = native(
  rust => "f64",
  typescript => "number",
  julia => "Float64"
)

[private]
[inline]
[rust_skip]
bool = native(
  rust => "bool",
  typescript => "boolean",
  julia => "Bool"
)

[private]
[inline]
str = native(
  rust => "String",
  typescript => "string",
  julia => "String"
)
