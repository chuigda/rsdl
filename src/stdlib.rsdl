[private]
[inline]
int = native(
  rust => "i64",
  typescript => "number",
  pl5 => "int"
)

[private]
[inline]
float = native(
  rust => "f64",
  typescript => "number",
  pl5 => "float"
)

[private]
[inline]
bool = native(
  rust => "bool",
  typescript => "boolean",
  pl5 => ""
)

[private]
[inline]
str = native(
  rust => "String",
  typescript => "string",
  pl5 => "string"
)
